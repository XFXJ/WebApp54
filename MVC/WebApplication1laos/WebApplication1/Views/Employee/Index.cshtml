@model WebApplication1.ViewModels.EmployeeListViewModel
@{
    Layout = "~/Views/Shared/MyDa.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <form action="/employee/search" method="post"> 
    <input type="text" name="searchString" value=""> 
    <input type="submit" name="搜索">
    </form>
    <h2>@Model.UserName,@Model.Greeting</h2>
    <p> @Html.ActionLink("新增", "addnew")
    @*<a href="/employee/addNew">新增</a>*@
    </p>
    <div> 
        <ul>
            @foreach (var item in Model.EmployeeViewList)
            {
                <li>@item.EmployeeName,@item.EmployeeSalary,@item.EmployeeGrade@*<a href="/employee/delete/@item.EmployeeId">删除</a>  <a href="/employee/Edit/@item.EmployeeId">更新</a>*@

                    @Html.ActionLink("更新", "Edit", new { id = item.EmployeeId }) |
                    @Html.ActionLink("删除", "Details", new { id = item.EmployeeId }) |
             </li>
            }
        </ul>
    </div>
</body>
</html>
